@page "/admin/category/category-frame-style"
@model do_an_ltweb.Admin.AdCategory.AdCategoryFrameStyle.IndexModel
@{
    ViewData["Title"] = "List CategoryFrameStyle";
}
<h2>@ViewData["Title"]</h2>

<partial name="_StatusMessage" model="@Model.StatusMessage"></partial>

<div class="mb-2">
    <a class="btn btn-secondary" asp-page="./Create">Create New CategoryFrameStyle</a>
</div>
<div class="card mb-4">

    <div class="card-header">
        <i class="fas fa-table me-1"></i>
        DataTable CategoryFrameStyle
    </div>
    <div class="card-body">
        <table id="datatablesSimple">

            <thead>
                <tr>
                    <th>NameCategoryFrameStyle</th>
                    <th>Hide</th>
                    <th>Tasks</th>
                </tr>
            </thead>

            <tfoot>
                <tr>
                    <th>NameCategoryFrameStyle</th>
                    <th>Hide</th>
                    <th>Tasks</th>
                </tr>
            </tfoot>

            <tbody>
                @foreach (var categoryFrameStyle in Model.CategoryFrameStyles)
                {
                    <tr>
                        <td>@categoryFrameStyle.NameCategory</td>
                        <td>@categoryFrameStyle.Hide</td>
                        <td>
                            <a class="btn btn-primary btn-sm" asp-page="./Edit" asp-route-framestyleid="@categoryFrameStyle.IdCategory">Update</a>
                            <a class="btn btn-danger btn-sm" asp-page="./Delete" asp-route-framestyleid="@categoryFrameStyle.IdCategory">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
